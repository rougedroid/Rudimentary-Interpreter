Okay, so first we make it interpret a singular line. That's it. Process it into tokens and then interpret it simple asf.

Goal: 
To evaluate the following: 
5 + 6 
5 - 6
5/6
5*6
5 = 6
5 > 6
5 < 6

Twist, use qwertyui for operators to ensure you actually learn't something. 

operation order: BO-DMAS erqw

Token Types:
0. NULL
1. Constants
  1 -> digit ( integer )
  12 -> char
  13 -> float

2. Operators
3. Variable
9. Processed Token
Errors, also possible that the tokens returned by the tokenize function won't make it cuz the actual tokens struct doesn't have space made with malloc. Rest is syntax errors, deal with it. 

Okay, tokenize fixed, made it a pointer to array. Operations work, need to put everything in switch-case cuz its faster idk, and need to introduce float values and need to do python like behavior where we see if its a float or an int and do processing accordingly. 

Remaking tokenize function.

tokenizer remade, and its wonderful its all clean smooth logic no writing same shit code in like 3 places, its all logically controlled and i didn't repeat myself like 20 times. 
need to make a characterizer for float values and also add a keyword detection system to reclassify the token types.

need to process tokens in this order: 
  functions --> eval and assign value as a token type 1 value
  variables --> find value from variable array and replace with a type 1 token.



